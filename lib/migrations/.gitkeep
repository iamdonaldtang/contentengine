# Migration files live here. Naming: NNN_description.sql (e.g. 001_add_indexes.sql).
# lib/db.py picks them up automatically in numeric order and records applied
# versions in the `schema_migrations` table.
